package com.example.calculadoraimc.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Gemini - início
 * Prompt:
 * Estou desenvolvendo a interface de um aplicativo Android usando Jetpack Compose.
 * Crie para mim toda uma coleção de cores para minha UI, meu aplicativo será roxo/azul.
 * Crie cores que sigam os padrões de mercado, que siga todas as acessibilidades. Se possivel, utilize como referência a biblioteca TaiwlindCSS
 * Quero um visual moderno, com predominância de tons de roxo e azul, usando um gradiente como cor principal.
 * Vou precisar de cores para tipografia, fundo, cor primaria e secundaria e gere também gradientes
 * Me de cores de status para utilizar na UI também, procure quais são utilizados no tailwind.
 */


// Paleta Principal - Roxo / Indigo (Tailwind)
val PrimaryDark = Color(0xFF312E81)      // indigo-900
val PrimaryMedium = Color(0xFF3730A3)    // indigo-800
val PrimaryLight = Color(0xFF4F46E5)     // indigo-600

val AccentColor = Color(0xFF7C3AED)      // purple-600
val AccentLight = Color(0xFFA78BFA)      // purple-400
val AccentSoft = Color(0xFFDDD6FE)       // purple-200

// Cores de Fundo
val BackgroundDark = Color(0xFF020617)   // slate-950
val BackgroundLight = Color(0xFFF8FAFC)  // slate-50
val SurfaceColor = Color(0xFFFFFFFF)     // white
val SurfaceVariant = Color(0xFFF1F5F9)   // slate-100

// Cores de Texto
val TextPrimary = Color(0xFF0F172A)      // slate-900
val TextSecondary = Color(0xFF475569)    // slate-600
val TextOnPrimary = Color(0xFFFFFFFF)    // white
val TextOnAccent = Color(0xFFFFFFFF)     // white

// Cores de Status (IMC / alertas)
val GreenLevel = Color(0xFF10B981)       // emerald-500
val OrangeLevel = Color(0xFFF59E0B)      // amber-500
val RedLevel = Color(0xFFEF4444)         // red-500

// Cores dos Cards de Métricas
val GreenColor = Color(0xFFD1FAE5)       // emerald-100
val GreenColorDark = Color(0xFF047857)   // emerald-700

val YellowColor = Color(0xFFFEF3C7)      // amber-100
val YellowColorDark = Color(0xFFB45309)  // amber-700

// Cores Auxiliares
val BlueColor = Color(0xFF6366F1)        // indigo-500
val WhiteTag = Color(0xFFFFFFFF)
val BlackFont = Color(0xFF0F172A)        // slate-900
val GreyColor = Color(0xFFF1F5F9)        // slate-100
val DividerColor = Color(0xFFE2E8F0)     // slate-200

// Gradientes
val GradientStart = Color(0xFF7C3AED)    // purple-600
val GradientEnd = Color(0xFF6366F1)      // indigo-500

/**
 * Gemini - fim
 */